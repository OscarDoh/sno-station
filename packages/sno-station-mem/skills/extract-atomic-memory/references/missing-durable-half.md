## Missing durable half

You are shown one occurrence whose happening changes a current durable state. Return only the
standing claim that the same quoted user text directly supports, and nothing else: not the
occurrence again, and not a state the text merely implies. Return `{"records":[]}` when the text
supports no standing claim.
